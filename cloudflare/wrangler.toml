name = "compliance-ai-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# After deploying, your worker URL will be:
# https://compliance-ai-proxy.<your-cloudflare-subdomain>.workers.dev
#
# Run these commands to deploy:
#   npm install -g wrangler
#   wrangler login
#   wrangler secret put ANTHROPIC_API_KEY   # paste your sk-ant-... key when prompted
#   wrangler deploy
